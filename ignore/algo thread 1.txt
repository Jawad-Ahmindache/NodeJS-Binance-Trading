1 - On recup la liste api grace aux fichier
2 - On envoie les api sur la db sql listApiToDB
3 - On get la liste des api avec getListApiFromDB($taskID)
4 - dans le thread si isCurrencyAvailable == false on le supprime de la db avec deleteListAPIFromDB
5- Une fois tous les thread fini on appel threadMaker

- method launchArbitrage
6- On appelle getListApiFromDB($taskID) puis si le count < 2 on envoie envoie logDB('Main.launchArbitrage Impossible de lancer un arbitrage')
SINON
	Dans chaque thread on appelle getApiFromDB($taskID)
	boucle while count(getApiFromDB) > 0
		delApiDB($apiName)
		on getPrice puis on met dans la db var_getPrice si on obtient le prix
		on rappelle getApiFromDB



		
		
			
	
